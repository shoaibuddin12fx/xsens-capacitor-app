import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'xsens-capacitor-app',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
